import { CarouselHoverDirective } from './components/herois/carousel-hover.directive';

describe('CarouselHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselHoverDirective();
    expect(directive).toBeTruthy();
  });
});
